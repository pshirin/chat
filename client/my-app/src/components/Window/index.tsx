import "./Window.css";
import MessagesContainer from "../MeesagesContainer";

const Window = () => {
  return (
    <section className="window">
      <MessagesContainer />
    </section>
  );
};

export default Window;
